# Data Store Console Setup Guide

> Generated by `scripts/generate-setup-guide.js` — do not edit by hand.

**26 tables · 136 columns** to create in
**Catalyst console → Cloud Scale → Data Store → New Table**.

## Before you start

1. **Column names must match exactly** (including case). The seed script and the
   ZCQL query builders look columns up by name — a typo silently breaks that table.
2. **Do not create** `ROWID`, `CREATEDTIME`, `MODIFIEDTIME` or `CREATORID` —
   Catalyst adds those itself.
3. Leave **Mandatory** and **Unique** unchecked on every column. The `*ID` primary
   keys are ordinary Number columns we populate ourselves; Data Store does not
   enforce foreign keys, so referential integrity is maintained by the seed data.
4. **Booleans**: set the default to `false`.
5. Create tables in the order below — it follows dependency order, which is also
   the order the seeder inserts in.

## ⚠️ Name collision

The schema's `Accused` table clashes with the **existing `Accused` table**
(124 rows: `name`, `fir_count`, `is_repeat_offender`, …). To avoid destroying
working data mid-migration, the new one is named **`AccusedPerson`** here.
Everything downstream uses that name. Once the migration is verified you can
delete the old `Accused`, `FIRs`, `Alerts` tables.

---

## Lookup / master tables (19)

### 1. `State`  *(4 columns)*
States (Karnataka only for this dataset)

| Column | Data type | Notes |
|---|---|---|
| `StateID` | Number | PK |
| `StateName` | Text (100) |  |
| `NationalityID` | Number |  |
| `Active` | Boolean |  |

### 2. `District`  *(4 columns)*
All 31 Karnataka districts

| Column | Data type | Notes |
|---|---|---|
| `DistrictID` | Number | PK |
| `DistrictName` | Text (100) |  |
| `StateID` | Number | FK -> State |
| `Active` | Boolean |  |

### 3. `UnitType`  *(5 columns)*
Police Station / Circle Office / Sub-Division / District HQ

| Column | Data type | Notes |
|---|---|---|
| `UnitTypeID` | Number | PK |
| `UnitTypeName` | Text (100) |  |
| `CityDistState` | Text (50) |  |
| `Hierarchy` | Number |  |
| `Active` | Boolean |  |

### 4. `Unit`  *(8 columns)*
~50 police stations and higher units

| Column | Data type | Notes |
|---|---|---|
| `UnitID` | Number | PK |
| `UnitName` | Text (150) |  |
| `TypeID` | Number | FK -> UnitType |
| `ParentUnit` | Number | self-FK -> Unit |
| `NationalityID` | Number |  |
| `StateID` | Number | FK -> State |
| `DistrictID` | Number | FK -> District |
| `Active` | Boolean |  |

### 5. `Rank`  *(4 columns)*
Constable .. SP

| Column | Data type | Notes |
|---|---|---|
| `RankID` | Number | PK |
| `RankName` | Text (100) |  |
| `Hierarchy` | Number |  |
| `Active` | Boolean |  |

### 6. `Designation`  *(4 columns)*
IO / SHO / Beat Officer / Circle Inspector / Superintendent

| Column | Data type | Notes |
|---|---|---|
| `DesignationID` | Number | PK |
| `DesignationName` | Text (100) |  |
| `Active` | Boolean |  |
| `SortOrder` | Number |  |

### 7. `Employee`  *(12 columns)*
~50 officers (investigating officers referenced by cases)

| Column | Data type | Notes |
|---|---|---|
| `EmployeeID` | Number | PK |
| `DistrictID` | Number | FK -> District |
| `UnitID` | Number | FK -> Unit |
| `RankID` | Number | FK -> Rank |
| `DesignationID` | Number | FK -> Designation |
| `KGID` | Text (20) |  |
| `FirstName` | Text (150) |  |
| `EmployeeDOB` | Date |  |
| `GenderID` | Number |  |
| `BloodGroupID` | Number |  |
| `PhysicallyChallenged` | Boolean |  |
| `AppointmentDate` | Date |  |

### 8. `CaseCategory`  *(2 columns)*
FIR, UDR, Zero FIR, PAR

| Column | Data type | Notes |
|---|---|---|
| `CaseCategoryID` | Number | PK |
| `LookupValue` | Text (50) |  |

### 9. `GravityOffence`  *(2 columns)*
Heinous / Non-Heinous

| Column | Data type | Notes |
|---|---|---|
| `GravityOffenceID` | Number | PK |
| `LookupValue` | Text (50) |  |

### 10. `CrimeHead`  *(3 columns)*
8 major crime groups

| Column | Data type | Notes |
|---|---|---|
| `CrimeHeadID` | Number | PK |
| `CrimeGroupName` | Text (150) |  |
| `Active` | Boolean |  |

### 11. `CrimeSubHead`  *(4 columns)*
~20 sub-heads (Murder, Robbery, Cyber Fraud, ...)

| Column | Data type | Notes |
|---|---|---|
| `CrimeSubHeadID` | Number | PK |
| `CrimeHeadID` | Number | FK -> CrimeHead |
| `CrimeHeadName` | Text (150) |  |
| `SeqID` | Number |  |

### 12. `Act`  *(4 columns)*
IPC, NDPS, POCSO, IT Act, Arms Act

| Column | Data type | Notes |
|---|---|---|
| `ActCode` | Number | PK |
| `ActDescription` | Text (200) |  |
| `ShortName` | Text (50) |  |
| `Active` | Boolean |  |

### 13. `Section`  *(4 columns)*
~30 sections across the acts

| Column | Data type | Notes |
|---|---|---|
| `ActCode` | Number | FK -> Act |
| `SectionCode` | Text (30) | part of composite key |
| `SectionDescription` | Text (300) |  |
| `Active` | Boolean |  |

### 14. `CrimeHeadActSection`  *(3 columns)*
Maps crime heads to act/section

| Column | Data type | Notes |
|---|---|---|
| `CrimeHeadID` | Number | FK -> CrimeHead |
| `ActCode` | Number | FK -> Act |
| `SectionCode` | Text (30) | FK -> Section |

### 15. `CasteMaster`  *(2 columns)*
Official government caste category list

| Column | Data type | Notes |
|---|---|---|
| `caste_master_id` | Number | PK — lowercase per schema doc |
| `caste_master_name` | Text (100) |  |

### 16. `ReligionMaster`  *(2 columns)*
Official religion list

| Column | Data type | Notes |
|---|---|---|
| `ReligionID` | Number | PK |
| `ReligionName` | Text (100) |  |

### 17. `OccupationMaster`  *(2 columns)*
Official occupation list

| Column | Data type | Notes |
|---|---|---|
| `OccupationID` | Number | PK |
| `OccupationName` | Text (100) |  |

### 18. `CaseStatusMaster`  *(2 columns)*
Under Investigation / Charge Sheeted / Closed / Undetected

| Column | Data type | Notes |
|---|---|---|
| `CaseStatusID` | Number | PK |
| `CaseStatusName` | Text (100) |  |

### 19. `Court`  *(5 columns)*
~20 courts across the active districts

| Column | Data type | Notes |
|---|---|---|
| `CourtID` | Number | PK |
| `CourtName` | Text (200) |  |
| `DistrictID` | Number | FK -> District |
| `StateID` | Number | FK -> State |
| `Active` | Boolean |  |


---

## Transactional tables (7)

### 20. `CaseMaster`  *(18 columns)*
65 FIR cases — the spine of the schema

| Column | Data type | Notes |
|---|---|---|
| `CaseMasterID` | Number | PK |
| `CrimeNo` | Text (30) | 1+4+4+4+5 encoded, e.g. 104430006202600001 |
| `CaseNo` | Text (50) |  |
| `CrimeRegisteredDate` | DateTime |  |
| `PolicePersonID` | Number | FK -> Employee |
| `PoliceStationID` | Number | FK -> Unit |
| `CaseCategoryID` | Number | FK -> CaseCategory |
| `GravityOffenceID` | Number | FK -> GravityOffence |
| `CrimeMajorHeadID` | Number | FK -> CrimeHead |
| `CrimeMinorHeadID` | Number | FK -> CrimeSubHead |
| `CaseStatusID` | Number | FK -> CaseStatusMaster |
| `CourtID` | Number | FK -> Court |
| `IncidentFromDate` | DateTime |  |
| `IncidentToDate` | DateTime |  |
| `InfoReceivedPSDate` | DateTime |  |
| `latitude` | Double |  |
| `longitude` | Double |  |
| `BriefFacts` | Text (max length) | MAX length — case narrative |

### 21. `ComplainantDetails`  *(8 columns)*
One or more complainants per case

| Column | Data type | Notes |
|---|---|---|
| `ComplainantID` | Number | PK |
| `CaseMasterID` | Number | FK -> CaseMaster |
| `ComplainantName` | Text (150) |  |
| `AgeYear` | Number |  |
| `OccupationID` | Number | FK -> OccupationMaster |
| `ReligionID` | Number | FK -> ReligionMaster |
| `CasteID` | Number | FK -> CasteMaster |
| `GenderID` | Number |  |

### 22. `ActSectionAssociation`  *(5 columns)*
Acts/sections charged per case

| Column | Data type | Notes |
|---|---|---|
| `CaseMasterID` | Number | FK -> CaseMaster |
| `ActID` | Number | FK -> Act.ActCode |
| `SectionID` | Text (30) | FK -> Section.SectionCode |
| `ActOrderID` | Number |  |
| `SectionOrderID` | Number |  |

### 23. `Victim`  *(6 columns)*
Victims per case

| Column | Data type | Notes |
|---|---|---|
| `VictimMasterID` | Number | PK |
| `CaseMasterID` | Number | FK -> CaseMaster |
| `VictimName` | Text (150) |  |
| `AgeYear` | Number |  |
| `GenderID` | Number |  |
| `VictimPolice` | Boolean |  |

### 24. `AccusedPerson`  *(6 columns)*
124 accused rows. NOTE: schema calls this "Accused"; renamed to avoid colliding with the existing Accused table

| Column | Data type | Notes |
|---|---|---|
| `AccusedMasterID` | Number | PK |
| `CaseMasterID` | Number | FK -> CaseMaster |
| `AccusedName` | Text (150) |  |
| `AgeYear` | Number |  |
| `GenderID` | Number |  |
| `PersonID` | Text (10) | A1, A2, A3 ... within a case |

### 25. `ArrestSurrender`  *(12 columns)*
~14 arrest/surrender events

| Column | Data type | Notes |
|---|---|---|
| `ArrestSurrenderID` | Number | PK |
| `CaseMasterID` | Number | FK -> CaseMaster |
| `ArrestSurrenderTypeID` | Number | 1=Arrest, 2=Surrender |
| `ArrestSurrenderDate` | DateTime |  |
| `ArrestSurrenderStateId` | Number | FK -> State |
| `ArrestSurrenderDistrictId` | Number | FK -> District |
| `PoliceStationID` | Number | FK -> Unit |
| `IOID` | Number | FK -> Employee |
| `CourtID` | Number | FK -> Court |
| `AccusedMasterID` | Number | FK -> AccusedPerson |
| `IsAccused` | Boolean |  |
| `IsComplainantAccused` | Boolean |  |

### 26. `ChargesheetDetails`  *(5 columns)*
18 chargesheeted cases

| Column | Data type | Notes |
|---|---|---|
| `CSID` | Number | PK |
| `CaseMasterID` | Number | FK -> CaseMaster |
| `csdate` | DateTime |  |
| `cstype` | Text (1) | A=Chargesheet, B=False Case, C=Undetected |
| `PolicePersonID` | Number | FK -> Employee |


---

## When you're done

Tell me and I'll run the seeder, which verifies every table exists and reports
row counts per table before inserting anything.
